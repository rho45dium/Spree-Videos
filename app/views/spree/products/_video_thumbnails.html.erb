<% product.videos.each do |video| %>
<%= link_to image_tag(video.youtube_thumbnail_url, :width => 75), 'Javascript:void(0);', :title => video.youtube_title, "data-video-id" => video.youtube_ref %>
<% end if product.videos.length > 1 %>